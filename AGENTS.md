# Agent 工作约定

本文件只规定进入项目后的阅读顺序和协作边界，不重复架构与开发细节。

## 开始工作

1. 先检查与任务直接相关的代码、配置和测试；不要根据目录模板猜测当前实现。
2. 打开 [`docs/development/README.md`](docs/development/README.md)，只读取任务对应的指南。
3. 只有任务跨层、涉及依赖方向或职责归属时，才读取 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)。
4. 只有需要理解或改变既有决策时，才读取对应 ADR。
5. 不得默认读取 `docs/archive/`；归档只用于明确的历史追溯任务。

禁止把整个 `docs/` 目录一次性作为默认上下文。

## 事实与冲突

- 代码、配置和测试描述当前实现；`ARCHITECTURE.md` 规定当前生效的架构边界。
- 文档示例不是待开发需求，缺少某个示例目录也不表示需要补齐。
- 若实现与架构边界冲突，指出具体文件和规则，先确认应修代码还是改决策，不得静默猜测。
- 不得用归档文档裁决当前实现。
- 未经用户明确授权，不查看或引用其他项目来推断本项目设计。

## 改动落点

- 长期有效的分层或边界：更新 `docs/ARCHITECTURE.md`，并新增或替代 ADR。
- 具体开发方式：更新对应的 `docs/development/*.md`。
- 已接受决策发生变化：新增 ADR，并在旧 ADR 中标注被替代关系。
- 历史快照：保持不变，不做持续维护。

项目源码使用严格 TypeScript/TSX。新增业务目录和抽象必须由真实需求驱动，不预建空 Feature、Service 或组件清单。
